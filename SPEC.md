# 医记 Agent 版本正式开发规格

## 1. 背景与目标

医记是面向患者本人及家庭成员的病历资料整理工具。当前项目已经具备网页 App、后端 API、PostgreSQL、Redis、阿里云 ECS 测试部署、阿里云 OSS 适配、OCR 识别、病历资料归档、资料包导出等基础能力。

下一阶段目标是把医记从“病历管理工具”升级为“就诊资料整理 Agent”。第一版 Agent 不加入聊天窗口，不以对话问诊为核心，而是嵌入现有操作流程中，主动帮助用户完成资料识别、字段核对、分类归档、时间线整理、复诊待办提醒和就诊资料包生成。

医记 Agent 的定位是：

```text
帮用户把散乱的病历、报告、处方、影像资料整理成复诊前能用的资料包。
```

医记 Agent 明确不做：

```text
不提供诊断建议，不提供治疗建议，不提供用药建议，不替代医生判断。
```

成功标准：

- 用户可以上传多份病历资料，并在核对后形成可追溯的病历资料库。
- Agent 能根据资料状态主动提醒用户补全字段、生成时间线、准备资料包。
- 用户可以为本人或家庭成员生成医生和患者都能读懂的 PDF 就诊资料包。
- 所有原始资料统一存储在阿里云 OSS 私有存储中。
- 产品符合中国大陆正式上线的基础合规要求。

## 2. 用户和使用场景

### 主要用户

患者本人。用户需要长期保存自己的病历、检查报告、处方、复诊记录，并在下次就医前快速整理成清楚的资料。

### 次要用户

家庭成员管理者。用户可能需要帮父母、孩子或伴侣整理病历资料，并在就医前准备资料包。

### 间接受众

医生或接诊人员。医生不是系统的直接使用者，但资料包需要做到医生能快速阅读：本次目的、近期情况、时间线、关键资料、想问的问题、附件清单。

### 核心场景

用户拍照或上传病历资料后，Agent 自动识别文字和关键字段，提示用户核对必要信息。确认后资料进入病历资料库，并自动形成时间线节点。

用户复诊前，Agent 根据时间、分类和资料状态提醒用户补充近期情况、选择相关资料、生成就诊资料包。

用户管理家庭成员时，可以在不同成员之间切换，并让每个成员的资料、时间线、待办和资料包互相隔离。

## 3. 功能范围

第一版 Agent 范围包括：

- 邮箱验证码登录。
- 本人档案与家庭成员档案。
- 上传病历资料，支持图片、PDF、多文件。
- OCR 识别与 PDF 文本提取。
- Agent 辅助字段整理、分类和标题生成。
- 用户核对字段后归档。
- 病历资料按时间从新到旧展示。
- 就医时间线由归档资料自动生成，并支持用户手动添加一次就医记录。
- Agent 主动生成就医相关待办。
- 资料包生成流程。
- PDF 导出。
- 资料包分享链接。
- 阿里云 OSS 私有文件存储。
- 中国大陆上线所需的隐私、导出、删除、审计和备案要求。

## 4. 非目标

第一版不做以下内容：

- 不做聊天窗口。
- 不做医疗问诊 Agent。
- 不做疾病诊断、治疗建议、用药建议。
- 不做医院内部系统。
- 不做医生端工作台。
- 不做商业化订阅、支付、会员套餐。
- 不做原生 iOS 首版开发，只为后续原生 iOS 规划保留接口和产品结构。
- 不承诺 OCR 自动识别 100% 准确。
- 不自动覆盖用户已确认的关键医疗字段。

## 5. 完整用户流程

### 5.1 新用户流程

用户打开网页 App，使用邮箱验证码登录。首次登录后创建本人档案，填写基础信息。系统引导用户上传第一份病历资料或手动添加一次就医记录。

### 5.2 上传资料流程

用户点击“添加病历资料”，选择拍照、相册、文件或手动填写。用户可一次上传多个图片或文件，多页病历可以作为同一条资料保存。

系统保存原始文件到阿里云 OSS。随后执行 OCR 或 PDF 文本提取。Agent 根据识别结果生成资料名称、资料类型、就诊日期、医院/机构、科室、分类、病情描述、诊疗建议摘要等候选字段。

用户进入核对页。页面分为基础信息、病情描述、诊疗建议。字段来源需要标明是识别得到、用户填写、还是待补充。用户必须核对资料名称、资料类型、就诊时间、医院/机构、医生/科室、分类和备注后才能归档。

如果 OCR 失败，流程不能中断。系统提示识别不清，保留原件，提取可读关键词供用户选择，并允许用户手动填写关键字段后保存。

用户保存后，资料进入病历资料库，并同步形成时间线节点和可能的待办。

### 5.3 病历资料流程

用户在病历页查看所有资料。资料按时间从新到旧排列，支持搜索和按分类查看。每份资料提供查看原件、编辑字段、加入资料包、重新识别、删除等操作。

编辑资料时，用户可以修改标题、资料类型、日期、医院、科室、分类、病情描述、诊疗建议和备注。重新识别只能生成新建议，不能直接覆盖用户已确认字段。

### 5.4 时间线流程

时间线由已经归档的病历资料自动生成。用户可以查看全部时间线，也可以按分类查看，例如胃疼、牙齿、体检等，分类由用户自己命名。

每个时间节点显示发生了什么、对应哪些资料、是否已加入资料包。用户也可以手动添加一次就医记录，适用于没有原始资料但需要记录就诊经历的情况。

### 5.5 待办流程

Agent 根据资料状态主动生成待办。待办只保留就医相关任务，包括复诊提醒、资料补全、上传报告、生成资料包。

示例：

- 刚上传的资料缺就诊日期，提醒用户核对。
- 复诊日期临近，提醒用户生成本次就诊资料包。
- 资料包缺近期情况，提醒用户补充症状变化和想问医生的问题。
- 某个分类下有多份资料但未整理，提醒用户归入时间线。

### 5.6 资料包流程

用户选择生成资料包。流程包括：

1. 选择就诊目的。
2. 选择时间范围。
3. 选择相关资料。
4. 填写近期情况。
5. 填写想问医生的问题。
6. 选择是否附上原始病历。
7. 生成预览。
8. 导出 PDF。

资料包预览和导出 PDF 必须采用医生和患者都能读懂的版式，不能展示技术化 JSON。

### 5.7 家庭成员流程

用户可以添加家庭成员，并在本人和家庭成员之间切换。不同成员的病历资料、时间线、待办、资料包和权限必须隔离。

第一版支持同一登录账号下管理多个家庭成员档案。不支持跨账号授权、邀请和细粒度协作权限。

## 6. 功能需求

### 6.1 账号与登录

- 系统必须支持邮箱验证码登录。
- 验证码必须有有效期。
- 验证码不能明文存储。
- 登录后应创建会话。
- 用户可以退出当前会话。
- 后续可扩展手机号登录，但第一版不作为主路径。

### 6.2 成员档案

- 用户必须至少有一个本人档案。
- 用户可以添加家庭成员。
- 每个成员拥有独立的病历资料、时间线、待办和资料包。
- 切换成员后，页面数据必须切换到对应成员。

### 6.3 文件上传

- 支持图片、PDF、多文件上传。
- 多页病历可以作为同一条资料保存。
- 上传后原始文件必须保存到阿里云 OSS 私有存储。
- 文件上传失败时不能创建不完整资料。
- 文件大小、类型和数量必须有限制。

### 6.4 OCR 与字段提取

- PDF 可复制文本优先使用文本提取。
- 图片优先使用百度 OCR。
- OCR 只用于识别字段和文字，不提供诊断、治疗或用药建议。
- Agent 可以辅助分类、命名和整理。
- 识别不确定时字段应留空或标记待补，不得乱填。
- 用户确认前，识别结果不得视为正式资料。
- 每个识别字段应保留来源信息，便于追溯到原始资料或 OCR 文本。

### 6.5 病历资料管理

- 支持查看资料列表。
- 支持按时间从新到旧排序。
- 支持搜索。
- 支持按分类筛选。
- 支持查看原件。
- 支持编辑字段。
- 支持重新识别。
- 支持加入资料包。
- 支持删除资料。

### 6.6 时间线

- 时间线由归档资料自动生成。
- 支持全部和按分类查看。
- 支持用户手动添加一次就医记录。
- 时间节点应显示日期、事件标题、分类、相关资料和资料包状态。
- 时间节点可加入资料包。

### 6.7 待办

- 待办由资料状态、时间线状态、复诊时间和资料包状态触发。
- 待办类型限定为复诊提醒、资料补全、上传报告、生成资料包。
- 用户可以完成或忽略待办。
- 待办文案必须通俗，不使用技术术语。

### 6.8 资料包与 PDF

- 支持选择就诊目的、时间范围、相关资料、近期情况、想问医生的问题。
- 支持选择是否附上原始病历。
- 支持生成预览。
- 支持导出 PDF。
- 支持生成资料包分享链接。
- 分享链接必须支持有效期和撤回。
- PDF 内容包括本次就诊目的、近期情况、就医时间线、关键资料清单、想问医生的问题、附件清单。
- 用户选择附上原始病历时，原始资料应嵌入 PDF。
- PDF 不得展示内部 ID、JSON、技术字段。

### 6.9 Agent 主动能力

- Agent 可以主动发现缺失字段。
- Agent 可以建议资料分类。
- Agent 可以建议资料标题。
- Agent 可以建议时间线整理方式。
- Agent 可以提醒复诊前生成资料包。
- Agent 所有建议必须允许用户确认、修改或忽略。
- Agent 不得自动给出医疗诊疗建议。

### 6.10 隐私与安全

- 病历正文、诊断名称、药品名称、OCR 原文不得用于广告、推荐或模型训练。
- 埋点不得采集病历正文、诊断名称、药品名称、OCR 原文。
- 用户必须能导出自己的数据。
- 用户必须能删除资料。
- 系统必须保留关键操作审计。
- 分享资料包必须支持限时和撤回。

## 7. 数据输入和输出

### 输入

- 邮箱地址。
- 邮箱验证码。
- 用户档案信息。
- 家庭成员信息。
- 图片文件。
- PDF 文件。
- 手动填写的病历资料。
- 复诊日期。
- 近期情况。
- 想问医生的问题。
- 用户对 OCR 字段的确认或修改。

### 输出

- 成员档案。
- 病历资料记录。
- 原始文件存储地址。
- OCR 文本和识别字段。
- 用户确认后的字段。
- 病历资料时间线。
- 就医待办。
- 资料包预览。
- PDF 文件。
- 资料包分享链接。
- 操作审计日志。

### 关键数据对象

- User：登录用户。
- MemberProfile：本人或家庭成员档案。
- MedicalRecord：病历资料。
- MedicalRecordFile：原始文件。
- RecognitionTask：识别任务。
- RecognizedField：识别字段。
- TodoItem：待办。
- VisitPack：资料包。
- AuditEvent：审计事件。

## 8. 页面、交互或命令行为

### 页面结构

第一版网页 App 以移动端优先为主，底部导航保留：

```text
病历
待办
个人中心
```

病历页包含：

- 添加病历资料。
- 病历资料列表。
- 时间线。
- 资料包入口。

待办页包含：

- 复诊提醒。
- 资料补全。
- 上传报告。
- 生成资料包。

个人中心包含：

- 当前账号。
- 当前成员。
- 家庭成员管理。
- 隐私与安全说明。
- 数据导出。
- 删除资料或注销入口。

### 主要交互

- 主按钮每页只突出一个。
- 上传入口必须支持拍照、相册、选择文件、手动填写。
- 上传后直接进入识别结果核对页。
- 搜索应作为独立浮层或独立页面，不应破坏病历页布局。
- 病历资料卡片必须提供查看原件、编辑字段、加入资料包。
- 编辑页必须支持重新识别。
- 资料包只有点击生成预览后才展示预览内容。

### 命令

开发和验证命令：

```bash
npm run prisma:validate
npm run build
npm test
npm run check
```

阿里云测试部署命令：

```bash
cd /root
bash aliyun-free-ecs-deploy.sh
```

健康检查：

```bash
curl http://127.0.0.1:3001/v1/health/ready
curl -I http://127.0.0.1
```

备份：

```bash
/root/aliyun-backup.sh
```

HTTPS：

```bash
/opt/yiji/scripts/aliyun-enable-https.sh yijijiankang.cn www.yijijiankang.cn support@yijijiankang.cn
```

## 9. 异常状态和边界情况

- OCR 完全失败：保留原件，提示识别不清，允许手动填写。
- OCR 部分成功：展示已识别字段和待补字段。
- 字段置信度低：不得自动填入正式字段，需用户确认。
- 多文件上传部分失败：不得保存半完成资料。
- 原件读取失败：提示暂时无法打开，不删除资料。
- OSS 上传失败：提示保存失败，允许重试。
- 资料包无资料：提示先选择资料。
- 资料包缺近期情况：生成待办提醒补充。
- 用户切换成员：当前页面数据必须重新加载，不能混用成员数据。
- 会话过期：提示重新登录。
- 网络中断：保留本地页面状态，提示稍后重试。
- 删除资料：需要二次确认。
- 用户删除账号：需要明确说明影响，并执行数据删除或进入删除等待期。

## 10. 非功能需求

### 安全

- 所有需要用户身份的数据接口必须鉴权。
- 成员数据必须隔离。
- 原始文件必须私有存储。
- 密钥不得进入代码仓库。
- 生产环境必须使用 HTTPS。
- 操作审计必须记录关键行为。

### 隐私

- 必须符合中国大陆个人信息保护要求。
- 必须明确说明健康资料用途。
- 不得将病历正文、诊断名、药品名、OCR 原文用于广告、推荐或模型训练。
- 用户必须能导出和删除数据。

### 可用性

- 移动端优先。
- 页面文案通俗。
- 主流程不依赖用户理解 OCR、模型、数据库等技术概念。
- 识别失败不能阻断资料归档。

### 性能

- 常规页面首次可交互应控制在可接受范围内。
- 上传和识别应展示明确进度。
- 大文件应限制大小或压缩后处理。

### 可靠性

- PostgreSQL 必须定期备份。
- 数据库每天自动备份一次，备份保留 30 天。
- 严重故障时以 24 小时内恢复为第一版目标。
- 原始资料使用 OSS 存储。
- 应有健康检查。
- 应有错误监控或告警通道。

### 合规

- 中国大陆正式上线前必须完成 ICP 备案。
- 第一版不需要公安备案展示位；如后续监管或上线形式要求，再补充。
- 需要隐私政策和用户协议。
- 医疗健康敏感数据处理需要单独评审。

## 11. 验收标准

### 产品验收

- 用户可以完成邮箱验证码登录。
- 用户可以创建本人档案并添加家庭成员。
- 用户可以上传图片、PDF 和多文件资料。
- 上传资料后，原始文件进入阿里云 OSS。
- OCR 成功时展示识别字段。
- OCR 失败时允许手动归档。
- 用户核对后资料进入病历列表。
- 病历资料按时间从新到旧展示。
- 时间线能从归档资料自动生成。
- 用户可以手动添加就医记录。
- Agent 能生成至少三类待办：资料补全、复诊提醒、生成资料包。
- 用户可以生成资料包预览并导出 PDF。
- 用户可以生成限时资料包分享链接，并可撤回。
- 用户选择附上原始病历时，导出的 PDF 包含嵌入的原始资料。
- PDF 内容不包含技术 JSON。

### Agent 验收

- Agent 不出现诊断、治疗、用药建议。
- Agent 建议必须可确认、修改或忽略。
- Agent 可以识别缺失字段并生成待办。
- Agent 可以建议分类和标题，但不直接覆盖用户已确认字段。
- Agent 输出能追溯到对应资料或用户输入。

### 技术验收

- `npm run check` 通过。
- 阿里云 ECS 健康检查返回 ok。
- PostgreSQL 和 Redis 正常。
- Nginx 返回首页 200。
- OSS 配置后原件可上传、读取、删除。
- 备份脚本可手动执行成功。
- HTTPS 可通过域名访问。

### 合规验收

- 隐私政策存在并可访问。
- 用户协议存在并可访问。
- 产品文案明确不提供诊疗建议。
- 埋点不采集敏感医疗正文。
- 用户可以导出和删除数据。
- 正式公开服务前完成 ICP 备案。

## 12. 已确认决策

- 写 Agent 版本规格。
- 第一版不加入聊天窗口。
- Agent 可以主动提醒用户。
- 坚持不提供诊断、治疗和用药建议。
- 目标平台以网页 App 为主，后续规划原生 iOS。
- 文件存储正式版统一使用阿里云 OSS。
- OCR 策略确认：PDF 文本提取优先，图片优先百度 OCR，识别不确定则用户核对。
- 第一版可以添加家庭成员。
- 第一版支持导出 PDF。
- 第一版需要资料包分享链接。
- 原始资料作为附件导出时嵌入 PDF。
- 家庭成员第一版仅支持同账号内管理，不支持跨账号授权；后端邀请/授权接口也应禁用。
- 备份策略为每天自动备份一次，保留 30 天，严重故障时目标 24 小时内恢复。
- OCR 测试集由用户提供真实资料、类型和人工标准答案。
- ICP 备案主体按个人备案处理。
- 第一版不需要公安备案展示位。
- 正式错误监控使用阿里云日志服务。
- 合规范围按中国大陆上线要求写。
- 第一版不写商业化功能。

## 13. 暂定假设

- 第一版登录方式为邮箱验证码登录。
- 手机号登录不进入第一版主路径。
- PDF 可选择是否附上原始资料，选择后原始资料嵌入 PDF。
- OCR 第一轮不承诺医疗级准确率，需要通过测试集评估后再升级。
- 正式上线前会购买符合备案要求的中国大陆服务器，不使用免费试用 ECS 作为长期正式备案服务器。
- 原生 iOS 只做后续规划，不影响当前网页 App 规格。

## 14. 未解决问题

- 备份恢复演练的执行时间未定；第一版上线前至少完成一次恢复演练。

## 技术栈

- 前端：原生 HTML / CSS / JavaScript，移动端优先网页 App。
- 后端：NestJS + TypeScript。
- 数据库：PostgreSQL。
- 缓存和验证码状态：Redis。
- ORM：Prisma。
- 文件存储：阿里云 OSS 私有存储。
- 邮件：Resend 或后续替换为中国大陆兼容邮件服务。
- OCR：PDF 文本提取 + 百度 OCR，后续按评测决定是否升级医疗 OCR。
- 部署：阿里云 ECS + Nginx + PM2。

## 项目结构

```text
app.html                         → 当前网页 App
index.html                       → 原型展示页
apps/api/src                     → 后端 API 源码
apps/api/test                    → 后端测试
prisma/schema.prisma             → 数据模型
scripts                          → 部署、OCR 测试、备份、运维脚本
privacy.html                     → 隐私政策
terms.html                       → 用户协议
SPEC.md                          → 当前开发规格
ALIYUN_PRODUCTION_TODO.md        → 阿里云上线操作清单
```

## 代码风格和边界

### Always

- 修改实现前先更新本规格或任务说明。
- 保持移动端优先。
- 医疗字段必须用户确认后才归档。
- 所有用户数据接口必须鉴权。
- 敏感密钥只能放环境变量或服务器配置文件。
- 修改后运行 `npm run check`。

### Ask first

- 修改数据库 schema。
- 增加新的第三方服务。
- 改登录方式。
- 改合规策略。
- 增加付费功能。
- 引入聊天窗口。

### Never

- 不提交密钥。
- 不把 OCR 结果当成医疗结论。
- 不提供诊断、治疗、用药建议。
- 不将病历正文用于广告、推荐或模型训练。
- 不删除用户资料而没有确认。
- 不在 SPEC 未确认前开始写 Agent 实现代码。
