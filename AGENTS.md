# 医记项目开发规则

本文件约束当前项目中的 Codex/Agent 开发行为。适用范围为本目录及其子目录。

## 1. 项目目标

医记是面向患者本人和家庭成员管理者的病历整理网页 App。当前开发目标是医记 Agent 版本第一版：不做聊天窗口，而是在上传、识别、核对、归档、时间线、待办和资料包流程中主动帮助用户整理资料。

第一版必须坚持：

- 不提供诊断建议。
- 不提供治疗建议。
- 不提供用药建议。
- 不替代医生判断。
- 生产文件存储使用阿里云 OSS。
- 错误监控使用阿里云日志服务。
- 数据库每天备份一次，保留 30 天。
- 第一版只支持同账号内家庭成员管理，后端禁用跨账号邀请和授权。

## 2. 文档优先级

开发时按以下顺序处理文档冲突：

1. 用户当前明确指令。
2. `SPEC.md`。
3. `docs/TECHNICAL_DESIGN.md`。
4. `IMPLEMENTATION_PLAN.md`。
5. `tasks/plan.md`。
6. `tasks/todo.md`。
7. 其他历史文档、原型和说明文件。

如果 `SPEC.md` 与现有代码冲突，先指出冲突并等待确认，不要自行决定。

如果开发计划与 `SPEC.md` 冲突，以 `SPEC.md` 为准，并先更新计划文档。

## 3. 开发流程

每次编码前必须明确：

- 本次任务对应 `tasks/todo.md` 中哪一项。
- 输入是什么。
- 输出是什么。
- 完成条件是什么。
- 验证方法是什么。

推荐执行顺序：

1. 读 `SPEC.md` 确认产品边界。
2. 读 `docs/TECHNICAL_DESIGN.md` 确认技术方案。
3. 读 `IMPLEMENTATION_PLAN.md` 或 `tasks/plan.md` 确认依赖关系。
4. 读 `tasks/todo.md` 确认当前最小任务。
5. 做最小必要修改。
6. 运行对应验证。
7. 汇报修改文件、验证结果和剩余风险。

禁止跳过计划直接做大范围重构。

## 4. 代码规范

### 通用规范

- 使用最小改动完成任务。
- 不做未被要求的抽象、框架迁移或大重构。
- 不改无关文件。
- 不删除用户已有内容，除非任务明确要求。
- 匹配现有项目风格。
- 新增能力必须有清晰命名和可验证边界。

### 后端规范

- 后端使用 NestJS + TypeScript + Prisma。
- 新接口必须明确鉴权边界。
- 涉及成员资料的数据必须按当前用户和 memberId 隔离。
- 分享链接只能保存 token hash，明文 token 只能返回一次。
- 文件读取、分享访问、删除、导出等关键操作必须记录审计。
- Agent 输出只能是资料整理、补全提醒、复诊准备、资料包准备，不得输出医疗判断。
- OCR 结果是候选字段，不能自动覆盖用户已确认字段。

### 前端规范

- 当前主 App 页面是 `app.html`，原型 `index.html` 应保留。
- 网页 App 移动端优先。
- 底部导航保持简洁，当前主路径为病历、待办、个人中心。
- 页面文案要通俗，避免技术化、后台化、法律条文化。
- 用户主操作必须清楚，避免一屏堆太多按钮。
- 不出现原型案例数据。
- 不出现“医生工作台”“给医生查看”等偏医生端表达。

### 数据和文件规范

- 生产环境原始资料统一使用阿里云 OSS 私有存储。
- 本地磁盘只能作为开发兜底。
- 路径处理必须兼容 Windows 和 Linux/Mac。
- 不要手写硬编码绝对路径作为业务逻辑。
- Node 代码中优先使用 `path.join`、`path.resolve` 或已有配置。
- 不在代码、文档示例、测试输出中暴露真实 API Key、验证码、登录 token 或用户病历正文。

## 5. 测试要求

每个任务至少完成对应级别的验证。

### 基础验证

```bash
npm run prisma:validate
npm run build
npm test
```

如任务涉及全局检查，使用：

```bash
npm run check
```

### 手工验证

涉及前端流程时，必须用手机宽度验证：

- 登录。
- 上传资料。
- OCR 识别。
- 字段核对。
- 保存归档。
- 查看原件。
- 编辑字段。
- 删除资料。
- 时间线展示。
- 待办生成。
- 资料包生成。
- PDF 导出。
- 分享链接生成和撤回。

### 安全验证

涉及权限、文件、分享链接时，必须验证：

- 未登录不能访问私有接口。
- 当前用户不能访问其他用户资料。
- 撤回或过期分享链接不能访问。
- 删除资料后原件不可继续读取。
- 跨账号邀请/授权接口第一版不可使用。

### OCR 验证

OCR 不能直接宣称准确。必须通过测试记录验证：

- 上传成功率。
- 可读识别率。
- 关键字段准确率。
- 人工修改率。
- 流程成功率。

第一轮测试集由用户提供 30-50 份真实资料和人工标准答案。

## 6. Git 规则

- 不使用 `git reset --hard`。
- 不使用 `git checkout --` 丢弃用户修改，除非用户明确要求。
- 提交前先检查变更范围。
- 只提交与当前任务直接相关的文件。
- 提交信息应说明具体任务，不写泛泛的 “update”。
- 如果工作区已有无关改动，保留并绕开，不擅自整理。

建议提交信息格式：

```text
feat(agent): add visit pack share model
fix(auth): disable cross-account family invitation
docs(plan): update backup and monitoring decisions
test(ocr): add accuracy report fields
```

## 7. 敏感配置规则

以下内容只能放在环境变量或服务器安全配置中：

- 数据库连接串。
- Redis 连接串。
- 阿里云 OSS AccessKey。
- 百度 OCR API Key 和 Secret。
- 邮件服务 API Key。
- 登录 token。
- webhook secret。

禁止把真实密钥写入：

- 代码。
- Markdown 文档。
- 测试快照。
- 浏览器页面。
- Git 提交记录。

`.env.example` 只能放变量名和假示例值。

## 8. 禁止事项

- 禁止在用户确认 SPEC 前写代码。
- 禁止把 Agent 做成医疗问诊。
- 禁止输出诊断、治疗、用药建议。
- 禁止默认信任 OCR 结果。
- 禁止把低置信度 OCR 字段乱填进关键医疗字段。
- 禁止把病历正文、诊断名称、药品名称、OCR 原文用于广告、推荐或模型训练。
- 禁止公开暴露 OSS 原始文件直链。
- 禁止第一版开放跨账号家庭邀请、授权和协作。
- 禁止把原型案例数据带入正式 App。
- 禁止为了美观或方便修改无关模块。
- 禁止在未验证的情况下声称“已上线”“已成功”“准确率达到 90%”。

## 9. 完成汇报格式

每次完成任务后，汇报应包含：

- 完成了什么。
- 修改了哪些文件。
- 跑了哪些验证。
- 验证结果是什么。
- 还有哪些风险或待确认项。

如果验证失败，必须说明失败原因和下一步处理建议。
