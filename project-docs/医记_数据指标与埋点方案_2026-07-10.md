# 医记数据指标与埋点方案

**版本**：MVP Draft  
**日期**：2026-07-10  
**原则**：隐私优先、最小必要、只采集产品决策需要的数据  

## 1. 为什么需要埋点

医记当前最需要回答的不是“用户点击了多少按钮”，而是这些业务问题：

1. 用户是否能完成从注册到首份资料归档？
2. 用户是否理解“手动整理 + 就诊资料包”的产品价值？
3. 用户是否愿意持续维护病历、用药、指标和待办？
4. 用户是否真的在就诊前生成资料包？
5. 家庭成员和订阅是否带来增量价值？
6. 哪些步骤让用户流失、失败或不信任？

因此，MVP 埋点必须围绕业务闭环设计：

```text
访问 -> 注册/登录 -> 本人建档 -> 上传资料 -> 分类归档
-> 添加药品/指标/待办 -> 搜索/时间线查看
-> 生成就诊资料包 -> 导出 PDF -> 留存/订阅
```

## 2. 合规和隐私边界

医疗健康信息属于敏感个人信息范畴。埋点不得采集病历正文、药品名称、诊断名称、检查结果原文、医院名称原文、文件名原文、邮箱明文、身份证号、手机号、原始文件 URL、分享 token、验证码、Cookie、访问令牌。

埋点只允许采集以下类型：

- 匿名或伪匿名用户 ID。
- 成员数量、资料数量、药品数量、指标数量等计数。
- 文件类型、资料类型、操作结果、耗时、错误码等产品行为数据。
- 是否完成某步骤、是否开启提醒、是否导出 PDF 等布尔或枚举数据。
- 经白名单处理的页面、功能、按钮、套餐类型。

### 禁止采集字段

| Category | Examples | Rule |
|---|---|---|
| 病历正文 | 主诉、诊断、检查结论、医嘱原文 | 禁止 |
| 药品敏感信息 | 药品名称、剂量、用法用量原文 | 禁止 |
| 原始资料信息 | 文件名、文件 URL、图片/PDF 内容、分享 token | 禁止 |
| 直接身份标识 | 邮箱明文、手机号、身份证号、姓名 | 禁止 |
| 精确健康数值 | 血压具体值、血糖具体值、体重具体值 | 默认禁止；如确需分析，只采区间或是否填写 |
| 医院/医生 | 医院名、科室名、医生名 | 默认禁止；如确需分析，只采“是否填写” |

### 可采集替代字段

| Need | Do Not Collect | Collect Instead |
|---|---|---|
| 判断上传了什么资料 | 文件名、报告内容 | `record_type`, `file_mime_group`, `file_size_bucket` |
| 判断分类质量 | 医院名、诊断名 | `has_hospital`, `has_record_date`, `has_tags`, `required_fields_completed_count` |
| 判断用药功能使用 | 药品名称、剂量 | `medicine_created`, `has_reminder`, `schedule_frequency_bucket` |
| 判断指标功能使用 | 具体血压/血糖值 | `metric_type`, `has_value=true`, `source_type` |
| 判断资料包可用性 | PDF 内容 | `visit_pack_sections_count`, `included_records_count`, `export_result` |

参考边界：个人信息处理应有明确合理目的、与目的直接相关，并采取对个人权益影响最小的方式；医疗健康信息属于敏感个人信息，应按更高标准处理。正式上线前需由合规律师复核。

## 3. 北极星指标

### North Star Metric

**每月生成过至少 1 次就诊资料包的活跃用户数。**

定义：

```text
Monthly Visit-Pack Active Users =
在自然月内完成 visit_pack_export_succeeded >= 1 的去重用户数
```

为什么不用“上传资料数”作为北极星：

- 上传资料只是中间行为。
- 医记的最终价值是就诊前可用，而不是存文件。
- 资料包能同时验证资料整理、药品、指标、搜索、分类和 PDF 导出是否成立。

### Supporting Metrics

| Metric | Definition | Target |
|---|---|---:|
| 首次建档完成率 | `profile_created / signup_completed` | >= 70% |
| 首份资料归档率 | `first_record_archived / profile_created` | >= 60% |
| 首次归档耗时 | `record_archived_at - record_upload_started_at` | P50 <= 3 min |
| 资料包生成率 | `visit_pack_created / users_with_archived_record` | >= 50% |
| PDF 导出成功率 | `visit_pack_export_succeeded / visit_pack_export_started` | >= 95% |
| 第二份资料上传率 | 30-60 天内上传第 2 份资料用户占比 | >= 25% |
| 用药提醒使用率 | `medicine_reminder_enabled / medicine_created` | >= 20% |
| 家庭成员创建率 | 创建 >= 2 个成员的活跃用户占比 | >= 15% |
| 试用转订阅率 | `subscription_started / trial_started` | >= 3% |

## 4. MVP 漏斗

### Activation Funnel

| Step | Event | Success Definition |
|---|---|---|
| 1 | `signup_completed` | 用户完成邮箱验证并创建会话 |
| 2 | `profile_created` | 创建本人健康档案 |
| 3 | `record_upload_succeeded` | 首份文件上传成功 |
| 4 | `record_archived` | 填写基础分类并归档 |
| 5 | `visit_pack_created` | 创建就诊资料包 |
| 6 | `visit_pack_export_succeeded` | PDF 导出成功 |

### Care Loop Funnel

| Step | Event | Success Definition |
|---|---|---|
| 1 | `medicine_created` | 添加当前用药 |
| 2 | `medicine_reminder_enabled` | 开启服药提醒 |
| 3 | `metric_created` | 记录指标 |
| 4 | `todo_created` | 创建复查/用药/资料待办 |
| 5 | `todo_completed` | 完成待办 |

### Family Subscription Funnel

| Step | Event | Success Definition |
|---|---|---|
| 1 | `member_created` | 创建家庭成员 |
| 2 | `member_access_invited` | 发起授权邀请 |
| 3 | `member_access_accepted` | 成员接受授权 |
| 4 | `pricing_viewed` | 查看套餐页 |
| 5 | `checkout_started` | 发起订阅 |
| 6 | `subscription_started` | 订阅成功 |

## 5. Event Naming Rules

使用动词过去式或明确状态：

```text
domain_object_action
```

Examples:

- `auth_code_requested`
- `signup_completed`
- `profile_created`
- `record_upload_started`
- `record_upload_succeeded`
- `record_archived`
- `visit_pack_export_failed`

禁止使用含糊命名：

- `click_button`
- `submit`
- `page_event`
- `ai_success`

## 6. Global Event Properties

每个事件默认带这些字段：

| Property | Type | Notes |
|---|---|---|
| `event_id` | string | UUID |
| `event_name` | string | 事件名 |
| `event_time` | ISO datetime | 服务端时间优先 |
| `anonymous_id` | string | 未登录用户 |
| `user_id_hash` | string/null | 登录后伪匿名 ID，不用邮箱 |
| `session_id` | string | 会话级 ID |
| `device_type` | enum | mobile / desktop / tablet |
| `platform` | enum | web / pwa |
| `app_version` | string | 发布版本 |
| `environment` | enum | production / staging / local |
| `locale` | string | zh-CN 等 |
| `utm_source` | string/null | 白名单来源 |
| `referrer_domain` | string/null | 只存域名，不存完整 URL query |
| `result` | enum | success / failure |
| `error_code` | string/null | 标准错误码 |
| `duration_ms` | number/null | 操作耗时 |

## 7. MVP Event Dictionary

### Auth & Onboarding

| Event | Trigger | Required Properties |
|---|---|---|
| `auth_code_requested` | 用户请求邮箱验证码 | `method=email`, `result`, `error_code` |
| `auth_code_verified` | 验证码校验完成 | `method=email`, `result`, `error_code` |
| `signup_completed` | 首次创建账号 | `method=email` |
| `login_completed` | 已有用户登录 | `method=email` |
| `profile_create_started` | 进入本人建档页 | `entry_source` |
| `profile_created` | 创建本人档案成功 | `has_allergy`, `has_chronic_condition`, `focus_tags_count` |
| `onboarding_task_viewed` | 查看档案补全任务 | `completed_task_count`, `remaining_task_count` |
| `onboarding_task_completed` | 完成一个补全任务 | `task_type` |

### Records

| Event | Trigger | Required Properties |
|---|---|---|
| `record_upload_started` | 选择文件并开始上传 | `member_type`, `file_mime_group`, `file_size_bucket`, `entry_source` |
| `record_upload_succeeded` | 文件上传成功 | `member_type`, `file_mime_group`, `file_size_bucket`, `duration_ms` |
| `record_upload_failed` | 文件上传失败 | `file_mime_group`, `file_size_bucket`, `error_code`, `duration_ms` |
| `record_classification_started` | 进入分类表单 | `record_type_initial`, `entry_source` |
| `record_archived` | 完成归档 | `record_type`, `has_record_date`, `has_hospital`, `has_department`, `has_tags`, `required_fields_completed_count` |
| `record_updated` | 修改资料字段 | `field_group`, `record_type` |
| `record_deleted` | 删除单份资料 | `record_type`, `deletion_reason_type` |
| `record_search_performed` | 执行搜索 | `query_length_bucket`, `filters_count`, `result_count_bucket` |
| `timeline_viewed` | 查看时间线 | `member_type`, `filter_type`, `records_count_bucket` |

Do not collect:

- 原始搜索词。
- 医院名。
- 资料标题。
- 诊断或检查文本。

### Medicines

| Event | Trigger | Required Properties |
|---|---|---|
| `medicine_create_started` | 进入添加药品 | `entry_source` |
| `medicine_created` | 药品保存成功 | `has_specification`, `has_usage_text`, `has_start_date`, `has_end_date`, `has_source_record` |
| `medicine_reminder_enabled` | 开启提醒 | `reminder_frequency_bucket`, `times_per_day_bucket` |
| `medicine_reminder_disabled` | 关闭提醒 | `reason_type` |
| `medicine_stopped` | 停用药品 | `has_stop_reason` |

Do not collect:

- 药品名称。
- 剂量、用法用量原文。
- 疾病或用途备注原文。

### Metrics

| Event | Trigger | Required Properties |
|---|---|---|
| `metric_create_started` | 进入指标记录 | `metric_type` |
| `metric_created` | 指标保存成功 | `metric_type`, `source_type`, `has_note`, `member_type` |
| `metric_trend_viewed` | 查看趋势 | `metric_type`, `time_range` |

Do not collect:

- 具体数值。
- 备注原文。

### Todos

| Event | Trigger | Required Properties |
|---|---|---|
| `todo_created` | 创建待办 | `todo_type`, `has_due_date`, `has_reminder` |
| `todo_completed` | 完成待办 | `todo_type`, `days_to_complete_bucket` |
| `todo_postponed` | 延期待办 | `todo_type`, `postpone_days_bucket` |
| `today_viewed` | 查看今日页 | `todo_count_bucket`, `overdue_count_bucket`, `profile_completion_bucket` |

### Visit Pack

| Event | Trigger | Required Properties |
|---|---|---|
| `visit_pack_create_started` | 点击“我要去看医生” | `entry_source`, `member_type` |
| `visit_pack_reason_added` | 填写本次就诊目的 | `reason_length_bucket`, `has_questions_for_doctor` |
| `visit_pack_records_selected` | 选择资料 | `selected_records_count`, `record_types_count` |
| `visit_pack_supplement_added` | 填写近期情况 | `supplement_length_bucket`, `has_symptom_start`, `has_medication_change` |
| `visit_pack_previewed` | 预览资料包 | `sections_count`, `included_records_count`, `included_medicines_count`, `included_metrics_count` |
| `visit_pack_export_started` | 开始导出 PDF | `sections_count`, `included_records_count` |
| `visit_pack_export_succeeded` | PDF 导出成功 | `duration_ms`, `file_size_bucket`, `sections_count` |
| `visit_pack_export_failed` | PDF 导出失败 | `error_code`, `duration_ms` |

Do not collect:

- 就诊目的原文。
- 症状描述原文。
- 想问医生的问题原文。
- PDF 内容。

### Family & Permissions

| Event | Trigger | Required Properties |
|---|---|---|
| `member_created` | 创建家庭成员 | `member_type`, `relationship_type`, `is_minor` |
| `member_switched` | 切换成员 | `from_member_type`, `to_member_type` |
| `member_access_invited` | 发起授权邀请 | `role`, `scope_type`, `expires_enabled` |
| `member_access_accepted` | 接受授权 | `role`, `scope_type` |
| `member_access_revoked` | 撤销授权 | `role`, `scope_type`, `revoked_by_role` |
| `privacy_consent_granted` | 同意敏感信息处理 | `consent_type`, `policy_version` |
| `privacy_consent_withdrawn` | 撤回同意 | `consent_type`, `policy_version` |

### Data Rights

| Event | Trigger | Required Properties |
|---|---|---|
| `data_export_requested` | 请求导出 | `export_scope`, `includes_original_files` |
| `data_export_succeeded` | 导出完成 | `duration_ms`, `export_scope`, `file_size_bucket` |
| `data_export_failed` | 导出失败 | `error_code`, `export_scope` |
| `deletion_requested` | 请求删除资料/账号 | `deletion_scope`, `requires_second_factor` |
| `deletion_completed` | 删除完成 | `deletion_scope`, `duration_bucket` |

### Subscription

| Event | Trigger | Required Properties |
|---|---|---|
| `pricing_viewed` | 查看订阅页 | `entry_source`, `current_plan` |
| `plan_selected` | 点击套餐 | `plan_type`, `billing_period` |
| `checkout_started` | 发起支付 | `plan_type`, `billing_period` |
| `subscription_started` | 订阅成功 | `plan_type`, `billing_period`, `trial_used` |
| `subscription_cancelled` | 取消订阅 | `plan_type`, `reason_type` |

## 8. Derived Tables

### User Activation Table

每日聚合，按用户一行：

| Field | Definition |
|---|---|
| `user_id_hash` | 伪匿名用户 |
| `signup_date` | 注册日期 |
| `profile_created_at` | 本人建档时间 |
| `first_record_uploaded_at` | 首份资料上传时间 |
| `first_record_archived_at` | 首份资料归档时间 |
| `first_visit_pack_exported_at` | 首次资料包导出时间 |
| `activation_completed` | 是否完成资料包导出 |
| `time_to_first_archive_minutes` | 注册到首份归档耗时 |

### Monthly Health Archive Usage

| Field | Definition |
|---|---|
| `month` | 月份 |
| `active_users` | 当月活跃用户 |
| `visit_pack_active_users` | 当月导出资料包用户 |
| `records_uploaded` | 上传资料数 |
| `records_archived` | 归档资料数 |
| `medicines_created` | 新增药品数 |
| `metrics_created` | 新增指标数 |
| `todos_completed` | 完成待办数 |

## 9. Dashboards

### Dashboard 1：MVP Activation

回答：用户有没有跑通核心闭环？

- 注册完成数。
- 本人建档完成率。
- 首份资料上传率。
- 首份资料归档率。
- 资料包创建率。
- PDF 导出成功率。
- 各步骤 P50/P90 耗时。
- 各步骤失败错误码 Top 10。

### Dashboard 2：Care Loop

回答：用户是否持续维护？

- 人均资料数。
- 第二份资料上传率。
- 药品创建率。
- 用药提醒开启率。
- 指标记录率。
- 待办完成率。
- 今日页访问频次。

### Dashboard 3：Privacy & Trust

回答：隐私优先是否落地？

- 授权创建/撤销次数。
- 数据导出请求数和成功率。
- 删除请求数和完成率。
- 越权拦截次数。
- 分享撤销后访问失败次数。
- 敏感操作二次验证失败率。

### Dashboard 4：Subscription

回答：家庭版是否值得做？

- 套餐页访问来源。
- 家庭成员创建率。
- 授权邀请/接受率。
- 套餐选择分布。
- 试用转订阅率。
- 取消订阅原因。

## 10. Implementation Recommendations

### Client vs Server

前端埋点适合采集：

- 页面进入。
- 表单开始。
- 用户点击入口。
- 前端校验失败。

后端埋点必须采集：

- 登录成功/失败。
- 上传成功/失败。
- 归档成功。
- PDF 导出成功/失败。
- 授权、撤销、删除、导出。
- 支付/订阅。

核心业务指标以后端事件为准，前端事件只能辅助分析流失。

### Event Pipeline

MVP 可以先用数据库表或轻量事件表：

```text
product_events
- id
- event_name
- user_id_hash
- anonymous_id
- session_id
- properties_json
- occurred_at
- received_at
- environment
```

要求：

- 所有 properties 进入白名单校验。
- 丢弃禁止字段。
- 对邮箱等身份标识只保存哈希或不保存。
- 服务端补充 `received_at`。
- 失败事件也要记录，但只记录错误码，不记录请求正文。

### Error Codes

错误码必须标准化，便于分析：

```text
AUTH_CODE_EXPIRED
AUTH_CODE_INVALID
AUTH_RATE_LIMITED
UPLOAD_FILE_TOO_LARGE
UPLOAD_TYPE_UNSUPPORTED
UPLOAD_STORAGE_FAILED
RECORD_PERMISSION_DENIED
PDF_RENDER_FAILED
EXPORT_PERMISSION_DENIED
DELETE_TASK_FAILED
PAYMENT_FAILED
```

## 11. QA Checklist for Analytics

- [ ] 每个 P0 用户故事至少有 1 个成功事件和 1 个失败事件。
- [ ] Activation Funnel 能从事件直接计算。
- [ ] PDF 导出成功率能从后端事件直接计算。
- [ ] 不采集病历正文、药品名称、诊断名称、指标具体值。
- [ ] 搜索事件不采集原始搜索词。
- [ ] 分享 token、文件 URL、验证码、Cookie、Token 不进入埋点。
- [ ] 所有事件属性经过白名单校验。
- [ ] Staging 和 Production 数据隔离。
- [ ] 用户删除/注销后，分析数据只保留不可识别的聚合数据或按政策处理。

## 12. First Sprint Tracking Plan

第一阶段不要一次性埋所有事件，先埋核心闭环：

1. `signup_completed`
2. `profile_created`
3. `record_upload_started`
4. `record_upload_succeeded`
5. `record_upload_failed`
6. `record_archived`
7. `medicine_created`
8. `metric_created`
9. `todo_created`
10. `visit_pack_create_started`
11. `visit_pack_export_started`
12. `visit_pack_export_succeeded`
13. `visit_pack_export_failed`
14. `data_export_requested`
15. `deletion_requested`

这些事件足够回答 MVP 是否成立。

## 13. Source Notes

- 《中华人民共和国个人信息保护法》要求个人信息处理遵循合法、正当、必要、诚信原则，并限于实现处理目的的最小范围。
- 中国网信网对敏感个人信息的解读明确提到医疗健康信息属于敏感个人信息。
- 《网络数据安全管理条例》自 2025-01-01 起施行，正式公开服务前需要纳入数据安全和个人信息处理合规评估。

